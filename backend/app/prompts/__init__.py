"""Prompts package — all GPT-5 prompt templates live here as .txt or .jinja2 files.

Per coding rules: prompts are NEVER hard-coded inside agent logic.
Templates are loaded from this directory by agent modules at runtime.
"""
